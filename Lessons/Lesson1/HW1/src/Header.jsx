import React from 'react';
import UserMenu from './UserMenu';

function Header() {
    return(
        <header className="header">
            <UserMenu/>
        </header>
    );
};

export default Header;